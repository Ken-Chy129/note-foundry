# NoteFoundry v0.1 Acceptance Record

## 2026-07-29 local deployed engineering acceptance

Revision: working tree on `codex/v01-foundation`

Deployment: full Docker Compose stack behind Caddy at `http://localhost:8088`, with PostgreSQL 17 and the optional local MinIO profile.

This run proves the implemented v0.1 behavior and recovery path. It is not the final production release sign-off because no real GitHub OAuth application, public domain, or S3 bucket independent of the application host was available in the environment.

| Scenario | Result | Evidence |
| --- | --- | --- |
| Public `AI Agent` space and nested `Studies/Hermes Agent` directories | Passed | Created and read through owner and public APIs. |
| Five substantive Hermes Agent notes | Passed | Overview, architecture/runtime, tool loop, memory/context management, and reliability/evaluation retained stable IDs. |
| Markdown capabilities | Passed | Code, two Mermaid diagrams, formula, table, managed PNG attachment, Tags, and stable Note Links rendered from canonical Markdown. |
| Forward link and derived backlink | Passed | Followed a body Note Link in the deployed browser and observed published backlinks. |
| Draft/publish isolation | Passed | The owner draft title and `INCOMPLETE_DRAFT_ONLY_1785296740` marker remained absent from Published Content. |
| Chinese and English search | Passed | `上下文管理` found the memory note; `idempotency` found the tool-loop note in anonymous search. |
| Anonymous visibility | Passed | Owner APIs returned `401`; a private-space note returned `404` through the public API. |
| Trash restore | Passed | Reliability note was removed from public access, restored, republished, and kept ID `7b339b51-59cb-473a-84e9-f453ca507560`. |
| Mobile reading | Passed | Browser viewport 390×844; body client and scroll widths matched, with rendered Mermaid and a loaded 960×420 PNG. |
| Desktop editing | Passed | Browser viewport 1440×900; navigation, 448 px editor, 448 px preview, and 280 px details panel rendered without horizontal overflow or console warnings/errors. |
| Automatic encrypted upload | Passed locally | Durable `backup.create` Job succeeded and uploaded `notefoundry/daily/2026/07/notefoundry-2026-07-29.tar.gz.age` to MinIO. |
| Real restore | Passed locally | Restored into empty database `notefoundry_restore_v01_20260729` and an empty attachment directory. Source and target both contained 8 notes, 2 attachments, 14 Note Links, and 8 search projections; all five Hermes IDs and both files were present. |
| Encryption rejection | Passed | Restore with an incorrect passphrase failed during age recipient matching before database replacement. |
| Real GitHub sign-in on configured domain | Pending external acceptance | Requires the production OAuth application, Owner ID, and domain. |
| Independent offsite S3 upload/restore | Pending external acceptance | Local MinIO proves the S3 path but is not offsite from the application host. |

The repeatable API scenario is `scripts/run-v01-acceptance.sh`. It requires `SESSION_TOKEN` for an existing Owner session; `API_BASE` defaults to `http://localhost:8088`. `RESUME_EXISTING=1` rechecks the recorded fixture after an interrupted run.

## Production sign-off template

- Date and deployed Git revision:
- Public domain:
- GitHub Owner sign-in result:
- Independent S3 provider and bucket region (do not record credentials):
- Encrypted backup object key:
- Clean restore target:
- Restored note / attachment / link / search-projection counts:
- Mobile and desktop browser results:
- Final decision:
